<% if (title) { %>
    <h1><%= title %></h1>
    <% } else {
        res.redirect('/')
    } %>
    <div class="flash-message"><%- messages() %>
        <% if (errors) { %>
            <ul class="notice">
           <% errors.array().forEach(error => { %>
             <li><%= error.msg %></li>
          <%  }) %>
           </ul>
          <% } %>
    </div>
    <div class="addInvLinks">
        <a href ="/inv/add-classification" class="addLinks">Add New Classification</a>
        <a href ="/inv/add-inventory" class="addLinks">Add New Inventory</a>
    </div>

